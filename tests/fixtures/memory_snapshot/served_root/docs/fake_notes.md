# Fake notes (synthetic fixture)

This file is a synthetic, unmistakably-fake fixture used only by
`apps/api/tests/test_build_memory_snapshot.py` to exercise the served-content
manifest (P24.10 Slice 2). Its bytes are fixed and committed so the sha256
digest in the golden fixture snapshot is stable across machines and runs.

It describes nothing real about any actual codebase, module, experiment, or
dataset.
