# SYNTHETIC FIXTURE -- read as TEXT, never executed.
XAS_MAIN_GRID = "1000 1050 5 1100 1"
def ZzL3_xas 'cntSec nbrScan emission nbrFilter' '{
  # declares no newfile: this is the acquisition METHOD, not a measurement
}'
